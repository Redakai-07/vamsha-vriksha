/** Engine ids, kept in their own module to avoid import cycles. */
export const GENERATIONS_ENGINE_ID = "generations";
export const RADIAL_ENGINE_ID = "radial";

export type LayoutEngineId = string;
